{
  "mcpServers": {
    "aivar": {
      "type": "http",
      "url": "${user_config.base_url}/api/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.api_token}"
      }
    }
  }
}
